# Template only — do not fill this in and commit it.
#
# Local dev: ignore this file, use .env instead (see .env.example).
#
# Streamlit Community Cloud: paste this content (with real values) into
# your app's Settings → Secrets box in the Streamlit Cloud dashboard.
# It is never committed to git.

LLM_PROVIDER = "gemini"
GEMINI_API_KEY = "your_gemini_key_here"
ANTHROPIC_API_KEY = "your_anthropic_key_here"
